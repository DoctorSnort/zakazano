package kz.chaykin.zakazano.model

/**
 * Вид напитка. Необязателен: под эти три подходит далеко не всё, что пьют,
 * и заставлять выбирать «коктейль» для чая было бы глупо.
 *
 * Имена значений попадают в базу и в резервную копию — переименовывать нельзя.
 */
enum class DrinkType {
    COCKTAIL,
    BEER,
    TINCTURE,
}
